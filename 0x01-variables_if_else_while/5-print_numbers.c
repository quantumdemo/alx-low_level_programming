#include <stdio.h>
#include <stdlib.h>
#include <time.h>
/**
 * main - print single numbers, between 1-10
 *
 * Return: 0 (Success)
 */
int main(void)
{
	int n;

	for (n = 0; n < 10; n++)
	{
		printf("%d", n);
	}
	return (0);
}
