#include "main.h"
/**
 * print_alphabet_x10 - alphabet
 */
void print_alphabet_x10(void)
{
	char alpha;

	alpha = 'a';

	while (alpha <= 'z')
	{
		_putchar(alpha);
		alpha++;
	}
	a--;
	_putchar(10);
	alpha = 'a';
}
