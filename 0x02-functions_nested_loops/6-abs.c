#include "main.h"
/**
 * _abs - computes the absolute value of an integer.
 *  @n: An input number
 *  Description: nothing
 */
int _abs(int a)
{
	if(a >= 0)
		return (a);
	else
		return (r * -1);
}		
