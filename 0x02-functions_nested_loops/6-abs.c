#include "main.h"
/**
 * _abs - computes the absolute value of an integer.
 *  @a: An input number
 *  Description: nothing
 *  Return: Absolut value of number a
 */
int _abs(int a)
{
	if (a >= 0)
		return (a);
	else
		return (a * -1);
}	
