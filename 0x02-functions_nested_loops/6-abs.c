#include "main.h"
/**
 * _abs - computes the absolute value of an integer.
 *  @n: An input number
 *  Description: nothing
 */
int _abs(int)
{
	int a;

	if (a = 0; a >= 10; a++)
		abs(a);
	return (a);
}

		
