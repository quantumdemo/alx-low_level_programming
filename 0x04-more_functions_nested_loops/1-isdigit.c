#include <stdio.h>
#include "main.h"
/**
 * _isdigit - checks for a digit
 * @c: checks for a digit (0 through 9).
 * Return: 1or 0
 */
int _isdigit(int c)
{
	for (c = 0; c <= 9; c++)
	{
		return (1);
	}
	return (0);
}
