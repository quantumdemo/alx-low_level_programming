#include <stdio.h>
#include "main.h"
/**
 * _isdigit - checks for a digit
 * @c: checks for a digit 
 * 
 * Return: get 1 if c is digit 
 * get 0 otherwise
 */
int _isdigit(int c)
{
	for (c = 0; c <= 9; c++)
	{
		return (1);
	}
	return (0);
}
