#include <stdio.h>
#include "main.h"
/**
 * _strlen - returns the length of a string
 * @*s: string
 */
int _strlen(char *s)
{
	s[50] = "My first strlen!";
	int len;

	for (len = 0; s[len] != '\0'; ++i)
		printf("%d", s);
	return (0);
}
